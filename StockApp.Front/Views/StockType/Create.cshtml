@model CreateStockTypeModel
@{
    Layout = "_Layout";
}

<br />
<br />
<br />

<div class="container-fluid" style="padding-left: 300px; padding-right: 300px;">
    <div class="card bg-dark text-light">
        <h4 class="card-header">Yeni Kayıt</h4>
        @foreach (var modelState in ViewData.ModelState.Values)
        {
            foreach (var error in modelState.Errors)
            {
                <div class="alert alert-danger">@error.ErrorMessage</div>
            }
        }
        <div class="card-body">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="Name" class="control-label">Ad</label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <br />
                <div class="form-group">
                    <label asp-for="Approval" class="control-label">Approval</label>
                    <input asp-for="Approval" class="form-control" />
                    <span asp-validation-for="Approval" class="checkbox-danger"></span>
                </div>
                <br />
              
                <br />
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Kayıt Oluştur</button>
                    <a asp-action="List" class="btn btn-secondary">Listeye Geri Dön</a>
                </div>
            </form>
        </div>
    </div>
</div>
<br />
<br />
<br />



@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}